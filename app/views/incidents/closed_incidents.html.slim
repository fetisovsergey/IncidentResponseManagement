- if @incidents.present?
  = render partial: 'incidents/incidents_table', locals: { incidents: @incidents, title: 'closed_incidents' }
- else
  .ui.header.huge.centered = t('no_closed_incidents')
  br
